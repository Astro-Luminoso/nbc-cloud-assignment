model = "gpt-5.5"
service_tier="fast"
model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "read-only"

[project]
name = "nbc-cloud-assignment"

[features]
hooks = false
multi_agent = true
plugin_hooks = false
fast_mode = true

[agents]
max_threads = 6
max_depth = 1